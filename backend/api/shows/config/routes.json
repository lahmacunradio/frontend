{
  "routes": [
    {
      "method": "GET",
      "path": "/shows",
      "handler": "shows.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/shows/count",
      "handler": "shows.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/shows/:id",
      "handler": "shows.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/shows",
      "handler": "shows.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/shows/:id",
      "handler": "shows.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/shows/:id",
      "handler": "shows.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
